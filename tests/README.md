# Unit tests

Written using https://google.github.io/googletest/

To run them use following command (tested on Ubuntu)
```shell
cmake -S . -B build
```
```shell
cmake --build build
```
```shell
cd build && ctest
```
